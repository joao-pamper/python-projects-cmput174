#ask for user input and turn into type int
C = int(input("What is the temperature in Canada? "))
#operation for temperature
F = (C * 9 / 5) + 32
#used int(F) so the number would be presented as a integer
print(C, "degrees in Canada would be", int(F), "degrees in Springfield. D'oh!")
